server:
  port: 9093
spring:
  application:
    name: product-service
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/product-service-db
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
billing:
  app: This is billing App Product Service qa